import type { Framework7Parameters } from "framework7/types";
import routes from "./routes";

const f7parameters: Framework7Parameters = {
  name: "TestApp",
  theme: "auto",
  routes: routes,
};

export default f7parameters;